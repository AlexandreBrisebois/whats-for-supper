name: recipe-description-regeneration
parameters:
    - recipeId
tasks:
    - name: generate_description
      processor: GenerateDescription
      payload:
          recipeId: "{{ recipeId }}"
    - name: sync_recipe
      processor: SyncRecipe
      depends_on:
          - generate_description
      payload:
          recipeId: "{{ recipeId }}"
