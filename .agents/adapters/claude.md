# Claude Code mechanics

Use [AGENT.md](../../AGENT.md) as the shared protocol. `CLAUDE.md` is the root
route, and `pwa/CLAUDE.md` routes back to it for PWA work. Check applicable local
instructions under shared authority. Linked files are not evidence of automatic
host import; verify loading in a fresh session.

This adapter applies to Claude Code operating on the repository. Use its exposed
tools and native permission controls. If native sub-agents are available, use the
shared context and file-ownership rules; otherwise execute sequentially. Chat or
review use outside this host does not inherit its tool capabilities.
